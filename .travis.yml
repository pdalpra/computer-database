language: scala

jdk:
  - openjdk11

cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt
    - $HOME/.cache/coursier

script: sbt ci-checks

after_success:
  - bash <(curl -s https://codecov.io/bash)

# whitelist only master branch and tags
branches:
  only:
    - master
